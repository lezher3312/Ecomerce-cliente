RewriteEngine On

# Indica la subcarpeta real del proyecto
RewriteBase /Global-client/

# Ignora archivos y carpetas reales
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Enruta todo a index.php
RewriteRule ^ index.php [QSA,L]
